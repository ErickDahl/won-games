import { Meta, StoryObj } from '@storybook/react'

import { FormProfile } from '.'

export default {
  title: 'Molecules/FormProfile',
  component: FormProfile
} as Meta

export const Default: StoryObj = {}
