import { HeadingProps } from '.'

const headingBlackMock: HeadingProps = {
  color: 'black',
  linePosition: 'left',
  lineColor: 'secondary'
}

const headingWhiteMock: HeadingProps = {
  color: 'white',
  linePosition: 'left',
  lineColor: 'secondary'
}

export { headingBlackMock, headingWhiteMock }
