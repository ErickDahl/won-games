// import { render } from '@testing-library/react'
// import { Gallery } from '.'
// import { galleryMock } from './mock'

describe('<Gallery />', () => {
  it('should render the heading', () => {
    // const { container } = render(<Gallery {...galleryMock} />)
    // expect(container).toBeInTheDocument()
  })
})
